#include <iostream>
#include <cstring>

using namespace std;

int main() {
  char str[81];
  char modStr[81];
  char modStrR[81];
  cout << "Enter a string to compare whether or not it is a palindrome" << endl;

  cin.get(str, 81);
  cin.get();

  int ct = 0;
  for(int i = strlen(str)-1; i >= 0; i--){
    
  }
  
  return 0;
}
